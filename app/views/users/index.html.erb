<h1>Listing Pictures</h1>
<h1>Thumb View</h1>
<table>
  <tr>
    <th>Avatar file name</th>
    <th>Avatar content type</th>
  </tr>
#try url or thumb in do |user| do|:thumb| or do|url| or write method
<% #@users.each do |user| %>
<% @users.each do |user|%>
  <tr>
    <td><%=h user.avatar_file_name %></td>
    <td><%=h user.avatar_content_type %></td>
    <td><%=h user.avatar.url(:thumb) %>
      <%#= image_tag @user.avatar.url(:thumb) %>
    
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Load New Picture', new_user_path %>